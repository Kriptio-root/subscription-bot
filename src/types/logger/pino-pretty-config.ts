import type { IPinoPrettyConfiguration } from '../../interfaces'

const pinoPrettyConfig: IPinoPrettyConfiguration = {
  prettyPrint: true,
  colorize: true,
  translateTime: 'SYS:standard',
  ignore: 'pid,hostname',
}

export default pinoPrettyConfig
